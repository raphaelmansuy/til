# How to create a default configuration file

In the project directory create a `.prettierrc` file

And use this configuration:

```json
{
  "semi": false,
  "singleQuote": true,
  "trailingComma": "all"
}
```
